<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="wrap_content"
              android:orientation="vertical">
    <LinearLayout
                  android:layout_width="fill_parent"
                  android:layout_height="wrap_content">
     <Button android:id="@id/provinceButton"
                android:layout_width="fill_parent"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:textSize="16sp"
                android:textColor="@color/text_hint"
                android:text="选择省"
                android:singleLine="true"
                android:background="@drawable/select_left"/>

    <Button android:id="@id/cityButton"
            android:layout_width="fill_parent"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:textSize="16sp"
            android:textColor="@color/text_hint"
            android:text="选择市"
            android:singleLine="true"
            android:background="@drawable/select_middle"/>

    <Button android:id="@id/countyButton"
            android:layout_width="fill_parent"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:textSize="16sp"
            android:textColor="@color/text_hint"
            android:text="区/县"
            android:singleLine="true"
            android:background="@drawable/select_right"/>
    </LinearLayout>

    <com.wlqq.huodi.view.WrapHeightGridView
            android:id="@id/region_gridview"
            android:layout_height="wrap_content"
            android:layout_width="fill_parent"
            android:layout_marginTop="-1px"
            android:verticalSpacing="-1px"
            android:horizontalSpacing="-1px"
            android:numColumns="3"
            android:fadingEdge="none"
            android:visibility="gone"
            android:stretchMode="columnWidth"
            android:background="@drawable/region_gridview_bg"
            android:listSelector="@android:color/transparent"
            />

</LinearLayout>